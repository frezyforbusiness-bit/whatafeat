# Whatafeat — Phase 1 demo (browser-local data)
# No secrets are required for the current prototype.

# Public site URL (optional; useful for absolute links later)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Mark the deployment as a public demo (optional)
NEXT_PUBLIC_DEMO=1

# Phase 2+ (unused today)
# DATABASE_URL=
# AUTH_SECRET=
# BLOB_READ_WRITE_TOKEN=
